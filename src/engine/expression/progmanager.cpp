#include "progmanager.h"
#include "userprog.h"


Engine::Latom* Engine::ProgManager::getUserProg (std::string id){
    /*
    for(auto it = programs.begin(); it == programs.end(); ++it){
        if (it.Engine::UserProg::getId() == id)
        { return it.getId(); }
    }
    */
}

